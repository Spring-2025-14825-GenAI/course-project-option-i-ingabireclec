run data_processing.py(python data_processing.py) to be able to download,process and embed papers in the chromadb store and save papers into a folder called papers,you can run it once.
stramlit run app.py to get the multilingual app running
